<%= bootstrap_fields_for @account || Account.new(accountable: InstagramAccount.new) do |f| %>
    <%= f.fields_for :accountable do |ff| %>
        <div>
            <%= ff.text_field :username, label: "Instagram username" %>
        </div>
    <% end %>
<% end %>